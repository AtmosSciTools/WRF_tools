name: wrf_tools
channels:
  - conda-forge
  - defaults
channel_priority: strict

dependencies:
  - python=3.12
  - ipykernel
  - xarray
  - cartopy
  - netcdf4=1.7.2
  - gcsfs
  - intake-esm
  - xesmf
  - pip
  - wrf-python
  - python-wget

  - pip:
      - git+https://github.com/dniloash/Pywinter
